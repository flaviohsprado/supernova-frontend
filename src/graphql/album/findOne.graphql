query FindAlbum($id: String!) {
  findOneAlbum(id: $id) {
    title
    releaseDate
    numberOfSongs
    duration
    file {
      url
    }
    musics {
      title
      duration
      file {
        url
      }
    }
  }
}
