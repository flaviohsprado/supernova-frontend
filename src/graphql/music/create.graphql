mutation CreateMusic($music: CreateMusicDTO!, $file: Upload) {
    createMusic(music: $music, file: $file) {
        id
        title
        duration
        album {
            title
        }
    }
}
